---
import {TimelineExperienceIsland, type IData} from "./TimelineIsland"
import { getLangFromUrl, useTranslations } from "@/i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---
<TimelineExperienceIsland experiences={t("projects") as IData[]} client:load />